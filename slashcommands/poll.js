// const { client } = require("..");

//const run = async (client, interaction) => {
//    let poll = interaction.options.getString("poll") || interaction.reply({content: "No poll given! Poll canceled", ephemeral: true})

 //   if (!member) return interaction.reply("Invalid member")
  //  try {
    //    await member.timeout(duration, reason)
      //  return interaction.reply(`${member.user.tag} has been timed out for ${durations.find(d=> duration === d.value)?.name} with a reason of ${reason}`)
   // }
    //catch(err){
      //  if (err){
        //    console.error(err)
          //  return interaction.reply(`Failed to create poll`)
